enum ColorType {
    Active = 'active',
    Default = 'default',
    Error = 'error',
    Link = 'link',
    Primary = 'primary',
    Secondary = 'secondary',
    Success = 'success',
    Warning = 'warning',
}

export default ColorType;
