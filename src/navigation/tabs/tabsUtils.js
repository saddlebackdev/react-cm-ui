export const getTabsTotalWidth = (tabRefs) => {
    const tabRefsKeys = Object.keys(tabRefs);
    // console.log(tabRefsKeys);
    // const totalWidth = tabRefsKeys.reduce((total, currentValue) => {
    //     console.log(currentValue, tabRefs[currentValue].tab.offsetWidth);
    // }, 0);
    return 0;
}
