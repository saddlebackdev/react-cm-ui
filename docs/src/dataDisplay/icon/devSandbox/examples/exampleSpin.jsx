import {
    Icon,
} from 'react-cm-ui';
import React from 'react';

function ExampleSpin() {
    return (
        <div>
            <Icon spin type="activity" />
            <Icon spin type="award" />
            <Icon spin type="book-open" />
            <Icon spin type="user" />
            <Icon spin type="time" />
            <Icon spin type="check" />
        </div>
    );
}

export default ExampleSpin;
